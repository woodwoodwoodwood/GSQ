# Production config — Qwen3.6-35B-A3B (MoE)
# Uses paths relative to the working directory; set GSQ_RUNTIME in .env/shell if artifacts should leave the repo (see scripts/_common.sh).
#
# Model: Qwen/Qwen3.6-35B-A3B  (model_type: qwen3_5_moe)
#   Architecture confirmed via AutoConfig (HF config.json):
#   - 256 routed experts, 8 active per token
#   - 40 decoder layers
#   - Hybrid attention: layer_types list — "linear_attention" (GatedDeltaNet) + "full_attention" (every 4th)
#   - Shared expert per layer: shared_expert + shared_expert_gate (Linear, sigmoid gated)
#   - Expert weights: 3D fused tensors gate_up_proj[E, 2*I, H] / down_proj[E, H, I]
#   - Expert intermediate size: moe_intermediate_size=512, hidden_size=2048
#   - Multimodal shell: text_config + vision_config (only the text part is quantized)
#
# Qwen3.6 is architecturally identical to Qwen3.5 (same model_type qwen3_5_moe),
# so it reuses Qwen35MoeDistributedWrapper / Qwen35MoeWrapper with no new wrapper.
#
# NOTE: Requires transformers >= 5.3.0 for Qwen3.5/3.6 MoE support.
#
# Scaling guidance (4 GPUs/node):
#   1 node  ( 4 GPUs) — 64 experts/GPU, ~12h for 40 layers
#   2 nodes ( 8 GPUs) — 32 experts/GPU, ~6h  for 40 layers
#   4 nodes (16 GPUs) — 16 experts/GPU, ~3h  for 40 layers
#
# Scratch layout:
#   With GSQ_RUNTIME set, YAML may use paths like "${GSQ_RUNTIME}/checkpoints/...", "${GSQ_RUNTIME}/logs/...", etc. (expandvars in load_config).
#   Typical layout under <repo>/runtime (default GSQ_RUNTIME)
#     .hf/                              ← HF_HOME  (set in gsq.toml)
#     checkpoints/qwen36-35b/         ← per-layer .safetensors shards
#     logs/qwen36-35b/                ← Python training logs

model:
  name: "/data1/models/Qwen3.6-35B-A3B"
  device: "cuda"
  dtype: "bfloat16"

data:
  dataset_name: "open_thoughts"
  batch_size: 64
  num_samples: 4096
  max_length: 4096
  num_workers: 8
  val_samples: 128

quantization:
  gsq_bits: 2
  init_method: "gptq"
  gsq_enabled: true
  start_layer: 0
  self_attn: false
  std: 0.01
  temperature: [2, 0.05]
  scale: [100, 500]
  groupsize: 128
  strength: 6
  logits_dtype: "bfloat16"   # "float32" for higher precision logits (more memory)

training:
  num_epochs: 10
  device_microbatch_size: 4
  lr1: 0.0002          # logit params (masks/signs) learning rate
  lr2: 0.0001          # scales learning rate
  weight_decay: 1.0
  checkpoint_dir: "checkpoints/qwen36-35b"
  log_dir: "logs/qwen36-35b"
  act_cache_dir: "cache/act_cache"
  eval_baseline: true
  # ppl_eval_every_n_layers: 6

gptq:
  nsamples: 512
  wbits: 2
  sym: true
  trits: false
  percdamp: 0.1
  blocksize: 128
  groupsize: 128
  static_groups: false
  prunen: 0
  prunem: 0

wandb:
  enabled: false
  entity: "ist"
  project: "GSQ-multi"
